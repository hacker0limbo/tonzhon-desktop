import PlaylistGroup from '../../components/PlaylistGroup';
import PureMusicList from './pure-music-list';

// 纯音乐歌单列表
export default function Pure() {
  return <PlaylistGroup playlists={PureMusicList} />;
}
